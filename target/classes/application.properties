#Nombre de identificacion la aplicacion
spring.application.name=CRM-Gym

# URL de conexi�n a la bd con el nombre de al bd
spring.datasource.url=jdbc:postgresql://localhost:5432/CRM-Gym

# Nombre de usuario y contrase�a
spring.datasource.username=postgres
spring.datasource.password=SuperSecurePassword3000

# Configuraci�n del Dialecto de Hibernate para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Habilitar la visualizaci�n de las consultas SQL
spring.jpa.show-sql=true

# Formatear las consultas SQL mostradas
spring.jpa.properties.hibernate.format_sql=true

# Estrategia de nomenclatura f�sica para mejorar la legibilidad de la base de datos SQL
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Estrategia de generaci�n del esquema (none, update, create, create-drop)
spring.jpa.hibernate.ddl-auto=update

# Habilitar logging m�s detallado para SQL
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Habulitar loggin seguridad
logging.level.org.springframework.security=DEBUG
# evitar advertencias de error en el arranque spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT signing and verification secret key, Base64 encoded.
jwt.secret=uwh0Np6zLbbPz4sj9dfD1GJc7m9ztzKjUVQZrk6Dpcg=

# JWT token expiration time in milliseconds (1 hour).
jwt.expiration=3600000